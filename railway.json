{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfig": {
      "providers": ["node"]
    }
  },
  "deploy": {
    "startCommand": "node server.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
