name: Bedrock Server & Tunnel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Setup LocalToNet
      run: |
        wget https://localtonet.com/download/localtonet-linux-x64.zip
        unzip localtonet-linux-x64.zip
        chmod +x localtonet

    - name: Combine Server Parts
      run: |
        cd mc/minecraft
        cat bedrock_server_part_* > bedrock_server
        chmod +x bedrock_server

    - name: Run Server and Tunnel
      run: |
        # Tokeni sisteme tanıt ve arka plana at
        ./localtonet authtoken aqtbZxleyWAJD0B8iFVCvsrUMkjzO7YKo &
        sleep 5
        
        # Tüneli UDP 19132 portuyla başlat ve arka plana at
        ./localtonet udp --port 19132 &
        sleep 10
        
        # Sunucu dosyalarının olduğu klasöre gir ve başlat
        cd mc/minecraft
        LD_LIBRARY_PATH=. ./bedrock_server
